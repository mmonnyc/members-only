<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'info-box'%>
      <% end %>
      <%= render 'feed' %>
      <%= render 'suggested-posts'%>
    </div>
  </div>
</section>

